# ReFrame Validation Tests

This directory is a placeholder for future ReFrame regression test configurations.

ReFrame will be used for L2 (capability-claim validation) and L3 (performance
characterization) testing, complementing the L1 Ansible-based `stack_validate` role.

When ReFrame tests are added they should follow the validation matrix defined in:
`gpu-environment-blueprint/docs/validation-matrix.md`
